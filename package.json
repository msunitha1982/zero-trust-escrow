{
  "name": "zero-trust-file-escrow",
  "version": "1.0.0",
  "description": "Zero-Trust Verified File Escrow — an HTML file is held hostage until the counterparty uploads a video AND the creator visually verifies and approves it.",
  "main": "server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "multer": "^2.4.0"
  },
  "license": "MIT",
  "private": true
}
